import styled from '@emotion/styled';

export const MainDiv = styled.div`
  display: flex;
  justify-content: center;
  margin: auto;
  gap: 1rem;
  max-width: 800px;
  margin-top: 5rem;
  flex-wrap: wrap;
`